# 🚀 部署指南

## ✅ 建置已完成!

你的應用程式已成功建置到 `dist/` 目錄。

## 📦 部署選項

### 選項 1: GitHub Pages (推薦)

已自動配置 GitHub Actions 工作流程!

**步驟:**

1. **提交並推送程式碼:**
   ```bash
   git add .
   git commit -m "優化產品比較介面並新增自動部署"
   git push origin main
   ```

2. **啟用 GitHub Pages:**
   - 前往 GitHub repository 設定
   - 點選左側選單的 "Pages"
   - Source 選擇 "GitHub Actions"
   - 等待約 1-2 分鐘,自動部署完成

3. **存取你的網站:**
   - 網址: `https://tw092669-ctrl.github.io/Product-inquiry/`

### 選項 2: Vercel (最快速)

```bash
# 安裝 Vercel CLI
npm i -g vercel

# 部署
vercel --prod
```

### 選項 3: Netlify

```bash
# 安裝 Netlify CLI
npm i -g netlify-cli

# 部署
netlify deploy --prod --dir=dist
```

### 選項 4: 自己的伺服器

直接上傳 `dist/` 目錄下的所有檔案到你的網站伺服器即可。

## 🔧 本地預覽

在部署前可以先本地預覽:

```bash
npm run preview
```

然後開啟 http://localhost:4173

## 📝 注意事項

- ✅ 已設定 `base: './'` 支援各種部署環境
- ✅ 已包含 GitHub Actions 自動部署配置
- ✅ 支援 PWA (離線使用)
- ✅ 響應式設計 (手機、平板、桌面)

## 🎯 建議使用 GitHub Pages

因為:
- ✅ 完全免費
- ✅ 自動 HTTPS
- ✅ 自動部署 (push 就更新)
- ✅ 已經配置完成,開箱即用

---

**需要幫助?** 如有任何問題,請隨時詢問!
